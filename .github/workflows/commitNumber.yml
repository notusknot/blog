name: Commit Hash
on:
  push:
    branches: [ main ]

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: script
        run: ./.github/workflows/commit.sh
        shell: bash
